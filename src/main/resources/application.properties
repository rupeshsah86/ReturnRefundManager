# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/return_refund_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=rupesh
spring.datasource.password=Rupeshsah@5920
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings for Development
# 'update' will create/update tables based on entities. 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Boot Application Name
spring.application.name=return-refund-manager